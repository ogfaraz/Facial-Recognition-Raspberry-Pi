# ---------------------------------------------------------------
# PC-Compatible requirements (pc-compatible branch)
# Raspberry Pi / NCS / GPIO libraries have been removed.
# ---------------------------------------------------------------

# --- Face Recognition: InsightFace (ArcFace / buffalo_s) ---
# Model is downloaded automatically on first run (~100 MB to ~/.insightface/).
# On Orange Pi 5 (RK3588): swap onnxruntime for onnxruntime-rknpu2 or use RKNN Toolkit.
insightface>=0.7.3               # ArcFace + RetinaFace via ONNX Runtime
onnxruntime>=1.16.0              # CPU ONNX Runtime; swap for onnxruntime-gpu on CUDA
opencv-python>=4.13.0            # 4.13+ required by albucore (cv2.CV_8U); provides GUI
numpy>=1.21.0,<2.0               # keep <2.0 for onnxruntime compatibility

# --- Django web dashboard ---
Django>=3.2,<4.3
djangorestframework>=3.14.0
django-filter>=22.1
django-widget-tweaks>=1.4.12
Pillow>=9.0.0
requests>=2.28.0
pytz>=2022.1
six>=1.16.0

# --- Optional / dev helpers ---
django-extensions>=3.2.0
pyOpenSSL>=22.0.0
