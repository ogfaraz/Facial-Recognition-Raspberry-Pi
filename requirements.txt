# ---------------------------------------------------------------
# PC-Compatible requirements (pc-compatible branch)
# Raspberry Pi / NCS / GPIO libraries have been removed.
# ---------------------------------------------------------------

# --- Face Recognition (core) ---
# Same stack used on Raspberry Pi.
# dlib must be pinned to 19.24.x — these versions ship pre-built Windows wheels
# so no Visual C++ or CMake build is required on Windows.
# On the Pi, pip installs from the ARM wheel or builds from source normally.
dlib==19.24.6                    # pinned: has pre-built wheel for Win/Mac/Linux
face_recognition>=1.3.0          # wraps dlib — identical on PC and Pi
opencv-python>=4.5.0             # webcam capture & display
numpy>=1.21.0                    # numerical ops

# --- Django web dashboard ---
Django>=3.2,<4.3
djangorestframework>=3.14.0
django-filter>=22.1
django-widget-tweaks>=1.4.12
Pillow>=9.0.0
requests>=2.28.0
pytz>=2022.1
six>=1.16.0

# --- Optional / dev helpers ---
django-extensions>=3.2.0
pyOpenSSL>=22.0.0
