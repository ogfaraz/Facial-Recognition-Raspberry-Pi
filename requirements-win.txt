# ---------------------------------------------------------------
# PC-Compatible requirements (pc-compatible branch)
# Raspberry Pi / NCS / GPIO libraries have been removed.
# ---------------------------------------------------------------

# --- Face Recognition (core) ---
# Same stack used on Raspberry Pi.
# dlib 19.24.x — pre-built wheel for Windows from:
# https://github.com/z-mahmud22/Dlib_Windows_Python3.x
# Install manually on Windows: pip install dlib-19.24.1-cp311-cp311-win_amd64.whl
# On the Pi, pip builds from source normally.
dlib>=19.24.0                    # pre-built wheel available for Win x64 + Python 3.11
face_recognition>=1.3.0          # wraps dlib — identical on PC and Pi
opencv-python>=4.5.0             # webcam capture & display
numpy>=1.21.0,<2.0               # dlib 19.24.1 pre-built wheel requires numpy 1.x

# --- Django web dashboard ---
Django>=3.2,<4.3
djangorestframework>=3.14.0
django-filter>=22.1
django-widget-tweaks>=1.4.12
Pillow>=9.0.0
requests>=2.28.0
pytz>=2022.1
six>=1.16.0

# --- Optional / dev helpers ---
django-extensions>=3.2.0
pyOpenSSL>=22.0.0
